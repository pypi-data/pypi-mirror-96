#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

package_dir = \
{'': 'src'}

install_requires = \
['terminaltables == 3.1.0']

entry_points = \
{'console_scripts': ['wifi-scan = wifi_scan:main']}

setup(name='wifi-scan',
      version='0.0.1',
      description='Scan & report on visible access points (iwlist scan).',
      author='Jesse London',
      author_email='jesselondon@gmail.com',
      url='https://github.com/jesteria/wifi-scan',
      py_modules=['wifi_scan'],
      package_dir=package_dir,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.8',
     )
