from setuptools import setup, find_packages

setup(name='darwin-datalake',
      version='0.1',
      description='Darwin Python Package',
      long_description='Darwin Python Package',
      author='Darwin Engineering Team',
      packages=find_packages(),
      package_data={'darwin-datalake': []},
      install_requires=[],
      scripts=[],
      zip_safe=False)
