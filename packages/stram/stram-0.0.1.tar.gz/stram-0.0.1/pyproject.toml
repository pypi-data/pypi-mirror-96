[tool.black]
target-version = ['py37']
line-length = 90
skip-string-normalization = true
include = '\.pyi?$'
exclude = '''
(
    \.git
  | \.vscode
  | __pycache__
)
'''
[build-system]
requires = [
    "setuptools==53.0.0",
    "wheel",
]
build-backend = "setuptools.build_meta"