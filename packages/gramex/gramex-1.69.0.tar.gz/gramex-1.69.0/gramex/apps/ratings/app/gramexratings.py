def startup(*args, **kwargs):
    print('startup', args, kwargs)
    # TODO: Create table specified by db=
    # Schema: time (DATETIME), user.id (TEXT), key (TEXT), rating (INT), additional attributes
