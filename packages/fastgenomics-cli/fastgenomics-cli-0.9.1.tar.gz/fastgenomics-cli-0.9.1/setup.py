#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['fastgenomics_cli']

package_data = \
{'': ['*']}

install_requires = \
['fastgenomics >= 3', 'requests >=2', 'tqdm >=4', 'update_checker >= 0.18']

entry_points = \
{'console_scripts': ['fg-cli = fastgenomics_cli.fg_cli:run',
                     'fg-upload = fastgenomics_cli.fg_upload:main']}

setup(name='fastgenomics-cli',
      version='0.9.1',
      description='FASTGenomics CLI',
      author='FASTGenomics',
      author_email='team@fastgenomics.org',
      url='https://beta.fastgenomics.org',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
     )
