[tool.poetry]
name = "fpv-stack"
version = "0.1.1"
description = ""
authors = ["Mikhail Cherdakov <mishacherdakov@yandex.ru>"]

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]
pytest = "^5.2"
mypy = "^0.812"
flake8-mypy = "^17.8.0"

[tool.poetry.scripts]
fpv-stack = "fpv_stack.main:fpv_stack"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
