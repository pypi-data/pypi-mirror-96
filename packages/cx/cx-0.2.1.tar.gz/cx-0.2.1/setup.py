#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['cx']

package_data = \
{'': ['*']}

install_requires = \
['click', 'flanders==0.3.0']

entry_points = \
{'console_scripts': ['cx = cx:cli.cli']}

setup(name='cx',
      version='0.2.1',
      description='Compute nearest neighbor distances along the coast with a view angle.',
      author='Radovan Bast',
      author_email='radovan.bast@uit.no',
      url='https://github.com/bast/coastal-express',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
     )
