Setup::

  hc login
  hc whoami
  hc rounds
  hc init workdir
  cd workdir/code
  hc run


For python::

    mkvirtualenv name

    edit hashcode.yml to add venv to python binary

    install tools (click, attrs, ipdb, numpy, pandas, scipy,...)
